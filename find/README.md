## find
An implementation of a rudimentary `find` command in xv6-riscv using C. Only finds files (as in it's actual sense, i.e. it does not return directory names; even though directories are also files in xv6).

# usage 
```
find <starting_path> <name>
```
